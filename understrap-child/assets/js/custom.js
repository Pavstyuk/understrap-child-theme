document.addEventListener('DOMContentLoaded', () => {

    // Slider
    const slider = new SimpleSlider('.simple-slider', {
        speed: 1000,
        delay: 5000,
        autoplay: true,
        class: {
            wrapper: 'slider-wrapper'
        }
    });


});